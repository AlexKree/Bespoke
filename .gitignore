node_modules/
package-lock.json
.env
.env.local

# Variantes d'images (regenerees par scripts/build-images.mjs)
assets/_img/

# Artefacts de build (regeneres par scripts/build-stock-pages.mjs)
fr/stock/
en/stock/
sitemap.xml
robots.txt
stock-feed.xml
stock-feed.csv
stock-feed-google.xml

# NB : build-stock-pages.mjs reecrit aussi la liste crawlable entre les
# marqueurs <!-- STOCK:LIST:START/END --> de fr/stock.html et en/stock.html
# (fichiers versionnes). Le resultat est idempotent tant que le stock ne
# change pas ; apres un build local sur un stock modifie, verifier le diff
# avant de committer.
